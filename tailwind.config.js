/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/app.js', // Adjust based on the types of files you're using
    '.index.html', // Add this if you're using HTML templates
  ],
  darkMode: 'selector',
  theme: {
    extend: {},
  },
  plugins: [],
}

